import { AdressfilterPipe } from './adressfilter.pipe';

describe('AdressfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdressfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
